#include "main.h"

/**
 * _isupper - uppercase letters
 * @c: char to check the result
 *
 * Return: 0 or 1
 */

int _isupper(int cr)
{
	if (cr >= 'A' && cr <= 'Z')
	return (1);
	else
	return (0);
}

